<%= form_with(model: employee, local: true) do |form| %>
  <% if employee.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(employee.errors.count, "error") %> prohibited this employee from being saved:</h2>

      <ul>
      <% employee.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :work_time %>
    <%= form.text_field :work_time, id: :employee_work_time %>
  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :employee_name %>
  </div>

  <div class="field">
    <%= form.label :function %>
    <%= form.text_field :function, id: :employee_function %>
  </div>

  <div class="field">
    <%= form.label :age %>
    <%= form.text_field :age, id: :employee_age %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
